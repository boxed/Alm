[36m-- TYPE MISMATCH -------------------------------------------------- src/Main.elm[0m

The 1st pattern in this `case` causing a mismatch:

 6|     case n of
 7|[91m>[0m        "x" ->
 8|             1
 9| 
10|         _ ->
11|             2

The first pattern is trying to match strings:

    [33mString[0m

But the expression between `case` and `of` is:

    [33mInt[0m

[4mHint[0m: Want to convert a String into an Int? Use the [92mString.toInt[0m function!

These can never match! Is the pattern the problem? Or is it the expression?

