[36m-- UNFINISHED EXPOSING -------------------------------------------- src/Main.elm[0m

I was partway through parsing exposed values, but I got stuck here:

1| module Main exposing (x
                          [91m^[0m
I was expecting a closing parenthesis. Try adding a [92m)[0m right here?

[4mNote[0m: I can get confused when there is not enough indentation, so if you already
have a closing parenthesis, it probably just needs some spaces in front of it.

